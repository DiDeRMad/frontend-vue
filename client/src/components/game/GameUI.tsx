import React from 'react';

const GameUI: React.FC = () => {
  return (
    <div className="game-ui">
      {/* All game UI elements will be rendered here */}
    </div>
  );
};

export default GameUI;